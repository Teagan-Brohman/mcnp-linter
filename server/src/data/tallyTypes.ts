/** Tally types that bin by surface (F1, F2). */
export const SURFACE_TALLY_TYPES = new Set([1, 2]);

/** Tally types that bin by cell (F4, F6, F7, F8). */
export const CELL_TALLY_TYPES = new Set([4, 6, 7, 8]);

/** Point detector tally type (F5). */
export const POINT_DETECTOR_TALLY = 5;

/** Fission energy deposition tally type (F7). */
export const FISSION_ENERGY_TALLY = 7;
